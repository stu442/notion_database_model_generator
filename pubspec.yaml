name: ndmg
description: Notion Database Model Generator - A command-line tool for generating Dart models from Notion databases.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

executables:
  ndmg: ndmg

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  http: ^1.5.0
  args: ^2.7.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
